@model Localidades.Models.DistritoMunicipal

@{
    ViewBag.Title = "Details";
}

<h2>Detalles</h2>

<div>
    <h3>Distrito Municipal</h3>
    <hr />


    <h4>@Html.DisplayFor(model => model.Nombre)</h4>


</div>

<div>
    <table class="table">
        <thead class="thead-dark">
            <tr>
                <th scope="col">Sección</th>
                <th scope="col">Cantidad de Sectores</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var r in ViewBag.Datos)
            {
                <tr>
                    <td>@r.Nombre</td>
                    <td>@r.NumeroSectores</td>
                </tr>
            }
        </tbody>
    </table>




</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
